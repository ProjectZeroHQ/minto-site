document.addEventListener('DOMContentLoaded',()=>{const body=document.body,header=document.querySelector('[data-header]'),menu=document.querySelector('[data-menu]');const scroll=()=>header&&header.classList.toggle('is-scrolled',window.scrollY>16);scroll();window.addEventListener('scroll',scroll,{passive:true});if(menu){menu.addEventListener('click',()=>{const open=body.classList.toggle('menu-open');menu.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.site-nav a,.mobile-nav a').forEach(a=>a.addEventListener('click',()=>{body.classList.remove('menu-open');menu.setAttribute('aria-expanded','false')}))}const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el))});
